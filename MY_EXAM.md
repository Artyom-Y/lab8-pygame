## Ex.6 eating++
The square might get so big it stops moving, so we need a limit to how big it can get.
We could use `rect.size <= 2 * (max_square_size ** 2)` or something
Note: when threat get big, prey starts moving in a very chaotic manner

## Ex.8
didnt have time to do, but we need to think in terms of delta time here